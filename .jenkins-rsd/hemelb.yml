# default build across compilers and machines
- job-template:
    name: '{name}-heme'
    description: |
        Compiles, runs units tests, and runs regressions for several types of builds.
    disabled: false
    block-upstream: true
    builders:
        - shell: |
            #!/bin/bash -xel
            # Setup environment
            case $os in
                "LegionRHEL7")
                    module use ~ccsprsd/modulefiles
                    export MAKEFLAGS=-j4
                ;;
                "Mavericks")
                    case $compiler in
                        "gnu")
                            export CXX=g++-4.9
                            export CC=gcc-4.9
                        ;;
                        "clang")
                            export CC=/usr/bin/cc
                            export CXX=/usr/bin/c++
                        ;;
                    esac
                    export MAKEFLAGS=-j2
                ;;
            esac
            with_boost=$([[ "$os" == "LegionRHEL7" ]] && echo "ON" || echo "OFF")

            # cpp check first -- on legion, this must be loaded without tinyxml
            cd $WORKSPACE
            [[ "$os" == "LegionRHEL7" ]] && module load cppcheck
            filename=$WORKSPACE/cppcheck-result.xml
            cppcheck --enable=all --xml Code/*.cc Code/*.h $(ls -d Code/*/) 2> $filename
            [[ "$os" == "LegionRHEL7" ]] && module unload cppcheck

            # # configure and build dependencies
            [[ "$os" == "LegionRHEL7" ]] && module load jenkins/heme/$compiler
            dep_build_dir=$WORKSPACE/dependencies/build
            mkdir -p $dep_build_dir/fake_install && cd $dep_build_dir
            cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo \
                  -DCMAKE_INSTALL_PREFIX=$dep_build_dir/fake_install \
                  -DHEMELB_USE_BOOST=$with_boost \
                  $WORKSPACE/dependencies
            make

            # configure and build code
            build_dir=$WORKSPACE/build
            mkdir -p $build_dir && cd $build_dir
            cmake -DCMAKE_BUILD_TYPE=Debug                           \
                  -DCMAKE_INSTALL_PREFIX=$dep_build_dir/fake_install \
                  -DHEMELB_USE_BOOST=$with_boost                     \
                  -DHEMELB_USE_STREAKLINES='{streaks}'               \
                  -DHEMELB_USE_SSE3='{sse3}'                         \
                  -DHEMELB_STEERING_LIB='{steering}'                 \
                  $WORKSPACE/Code
            make

            # run cpp tests
            mkdir -p $build_dir/tests && cd $build_dir
            for testname in $WORKSPACE/build/unittests_*; do
              filename=$(basename $testname)
              mpirun -n 1 $testname -o $build_dir/tests/$filename.xml || true
            done

            # setup regressions
            reg_dir=$WORKSPACE/regression/hemelb-inputs
            entropy=hg@entropy.chem.ucl.ac.uk
            mkdir -p $(dirname $reg_dir)
            [[ -e $reg_dir ]] || hg clone ssh://$entropy/hemelb-input $reg_dir
            cd $reg_dir && hg update

            # run regression
            nodes=$([[ "$os" == "LegionRHEL7" ]] && echo "1" || echo "2")

            result_dir=$reg_dir/results
            hemelb=$build_dir/hemelb

            cd $reg_dir/diffTest/
            mpirun -n $nodes $hemelb -in config.xml -out $result_dir -i 0
            export PYTHONPATH="$WORKSPACE/Tools:$PYTHONPATH"
            python ./ImageComparison CleanImages $result_dir/Images
            python ./NumericalComparison CleanExtracted $result_dir/Extracted

    triggers:
        - '{trigger}'

    publishers:
        - cpp_unit_tests
        - slackers
        - cppcheck:
            pattern: "cppcheck-result.xml"
            graph:
                xysize: [500, 200]
            display:
                sum: false
                error: true

    parameters:
        - '{param}'

- project:
    name: 'steering_and_streaks'
    steering: 'basic'
    streaks: 'ON'
    sse3: 'OFF'
    trigger: 'nightly_and_hook'
    branch: origin/master
    refspec: +refs/heads/*:refs/remotes/origin/*
    param: dummy
    jobs:
        - '{name}-heme'

- project:
    name: 'master'
    steering: 'none'
    streaks: 'OFF'
    sse3: 'ON'
    trigger: 'nightly_and_hook'
    branch: origin/master
    refspec: +refs/heads/*:refs/remotes/origin/*
    param: dummy
    jobs:
        - '{name}-heme'

- project:
    name: 'pull-request'
    steering: 'none'
    streaks: 'OFF'
    sse3: 'ON'
    trigger: 'pull_request_trigger'
    branch: $sha1
    refspec: +refs/pull/*:refs/remotes/origin/pr/*
    param: sha1
    jobs:
        - '{name}-heme':
