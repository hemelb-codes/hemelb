#!/bin/bash
#$$ -wd $job_results
#$$ -q all.q
#$$ -pe openmpi $cores
#$$ -N $job_name
#$$ -e $job_results/${job_name}.error
#$$ -o $job_results/${job_name}.output
$run_prefix
#and run
/opt/openmpi/gfortran/1.4.3/bin/mpirun -np $cores $install_path/bin/unittests_hemelb -o 'tests.xml'
